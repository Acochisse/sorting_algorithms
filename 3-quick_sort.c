#include "sort.h"

/**
 * quick_sort - function that runs sort by the method of quicksort
 * @array: input array
 * @size: doubly linked list
 * 
 * 
 */
void quick_sort(int *array, size_t size)
{
    size_t *current, *temp;
    
}